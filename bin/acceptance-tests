#!/usr/bin/env ruby
require 'bundler/setup'
lib_dir = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
$LOAD_PATH.unshift(lib_dir) unless $LOAD_PATH.include?(lib_dir)
require 'fb/integration'
require 'optparse'

Fb::Integration::TestsCLI.new(ARGV).run
