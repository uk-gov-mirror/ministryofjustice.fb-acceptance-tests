version: 2.1

jobs:
  test:
    docker:
      - image: circleci/ruby:latest
    steps:
      - setup_remote_docker
      - checkout
      - run:
          name: Setup the repository
          command: bundle install
      - run:
          name: Run tests
          command: ./bin/acceptance-tests --setup --run

workflows:
  version: 2
  commit-workflow:
    jobs:
      - test
